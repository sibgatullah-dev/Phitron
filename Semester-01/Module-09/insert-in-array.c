#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int a[n+1];
    for (int i = 0; i < n; i++)
    {
        scanf("%d",a[i]);
    }
    int index,val;
    scanf("%d %d",index,val);

    a[in]
    
    return 0;
}